buildscript {
    repositories {
        mavenCentral()
        maven { url "https://maven.minecraftforge.net" }
        maven { url "https://jitpack.io" }
    }
    dependencies {
        // 現在も1.7.10を支え続けているGTNHチームの最強の修正版
        classpath 'com.github.GTNewHorizons:ForgeGradle:1.2.11'
        // Kotlinプラグインの依存関係を追加（バージョンは 1.4.32 あたりが安定します）
        //classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.4.32"

    }
}

apply plugin: 'forge'

// これ以降（version = "1.0" など）はそのまま

version = "1.0"
group= "com.SakuraMochi17.kaisatsumod"
archivesBaseName = "kaisatsumod"

minecraft {
    version = "1.7.10-10.13.4.1614-1.7.10"
    runDir = "eclipse"
}

dependencies {
    // libsフォルダに入れたjarファイルを読み込む
    compile fileTree(dir: 'libs', include: '*.jar')
}

processResources
        {
            // バージョン変更時にタスクを再実行するための設定
            inputs.property "version", project.version
            inputs.property "mcversion", project.minecraft.version

            // mcmod.info 内の変数を置換
            from(sourceSets.main.resources.srcDirs) {
                include 'mcmod.info'
                expand 'version':project.version, 'mcversion':project.minecraft.version
            }

            // mcmod.info 以外のリソースをコピー
            from(sourceSets.main.resources.srcDirs) {
                exclude 'mcmod.info'
            }
        }
tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
